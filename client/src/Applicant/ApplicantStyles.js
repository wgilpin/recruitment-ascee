import TableStyles from '../TableStyles';

const styles = {
  submit: {
    fontSize: 'large',
    padding: '6px',
    color: TableStyles.styles.header.color,
  },
  padded: {
    padding: 6,
  },
  tabHeader: {
    color: TableStyles.styles.header.color,
  },
  heading: {
    color: TableStyles.styles.header.color,
  },
  hr: {
    borderColor: '#555555',
    borderWidth: '1px',
  }
};

export default styles;
